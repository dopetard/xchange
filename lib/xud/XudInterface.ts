import BaseClient from '../BaseClient';

/**
 * A generic interface that can be used for multiple Lightning clients and implementations
 */
interface XudInterface extends BaseClient {}

export default XudInterface;
